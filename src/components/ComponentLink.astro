---
interface Props {
  name: string;
  link: string;
}

const { name, link } = Astro.props;
---

<a
  class="font-semibold text-lg text-gray-400 hover:text-emerald-500 duration-300"
  href={link}>{name}</a
>
