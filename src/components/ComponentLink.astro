---
interface Props {
  name: string;
  link: string;
}

const { name, link } = Astro.props;
---

<a
  class="font-semibold text-xl text-white pl-16 py-5 rounded-lg hover:bg-zinc-500 hover:bg-opacity-20"
  href={link}>{name}</a
>
