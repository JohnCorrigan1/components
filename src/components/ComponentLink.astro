---
interface Props {
  name: string;
  link: string;
}

const { name, link } = Astro.props;
---

<a
  class="font-semibold text-xl text-gray-400 pl-16 py-5 rounded-lg hover:text-emerald-500 duration-300 md:text-lg"
  href={link}>{name}</a
>
